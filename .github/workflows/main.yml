name: Update DNS Blocklists

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: blocklist-update
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build massdns
        run: |
          sudo apt-get install -y --no-install-recommends build-essential
          git clone --depth 1 https://github.com/blechschmidt/massdns /tmp/massdns
          make -C /tmp/massdns -j$(nproc)
          sudo mv /tmp/massdns/bin/massdns /usr/local/bin/massdns

      - name: Run
        run: |
          BLOCK_OUT="blocklists.txt"
          BLOCK_TMP="blocklists.tmp"
          RESOLVERS_FILE="resolvers.tmp"

          # Chỉ giữ resolver ổn định, bỏ OpenDNS/AdGuard hay bị REFUSED
          printf '%s\n' 1.1.1.1 1.0.0.1 8.8.8.8 8.8.4.4 9.9.9.9 149.112.112.112 > "$RESOLVERS_FILE"

          echo "Downloading..."
          curl -fsSL --max-time 90 --parallel --parallel-immediate \
            https://adguardteam.github.io/HostlistsRegistry/assets/filter_16.txt \
            https://adguardteam.github.io/HostlistsRegistry/assets/filter_1.txt \
            https://raw.githubusercontent.com/bibicadotnet/AdGuard-Home-blocklists/refs/heads/main/byme.txt \
            https://raw.githubusercontent.com/VeleSila/yhosts/master/hosts \
            https://badmojr.github.io/1Hosts/Lite/adblock.txt \
          | awk '{
              if (/^[[:space:]]*$/ || /^[!#;]/) next
              line = tolower($0)
              sub(/^@@\|\|?/, "", line); sub(/^\|\|?/, "", line)
              sub(/\^.*/, "", line);     sub(/[#!].*/, "", line)
              sub(/\/.*/, "", line);     sub(/:.*/, "", line)
              sub(/^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+[[:space:]]+/, "", line)
              gsub(/^[[:space:]]+|[[:space:]]+$/, "", line)
              if (line ~ /^[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?(\.[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?)+$/ && !seen[line]++) print line
            }' | sort -u > "$BLOCK_TMP"

          TOTAL=$(wc -l < "$BLOCK_TMP")
          echo "Total: $TOTAL"

          SERVFAIL_TMP="servfail.tmp"

          # Pass 1 – concurrency 2000, timeout 500ms, tự retry SERVFAIL 1 lần
          echo "Pass 1..."
          massdns -r "$RESOLVERS_FILE" -t A -o J -s 2000 --timeout 500 --retry SERVFAIL "$BLOCK_TMP" \
          | tee >(jq -r 'select(.status == "SERVFAIL") | .name | rtrimstr(".")' > "$SERVFAIL_TMP") \
          | jq -r 'select(.status == "NOERROR") | .name | rtrimstr(".")' > "$BLOCK_OUT"

          SERVFAIL_COUNT=$(wc -l < "$SERVFAIL_TMP")
          echo "Pass 1 done – SERVFAIL còn: $SERVFAIL_COUNT"

          # Pass 2 – retry SERVFAIL với concurrency thấp hơn
          if [[ "$SERVFAIL_COUNT" -gt 0 ]]; then
            echo "Pass 2..."
            massdns -r "$RESOLVERS_FILE" -t A -o J -s 500 --timeout 500 --retry SERVFAIL "$SERVFAIL_TMP" \
            | jq -r 'select(.status == "NOERROR") | .name | rtrimstr(".")' >> "$BLOCK_OUT"
          fi

          sort -u "$BLOCK_OUT" -o "$BLOCK_OUT"
          VALID=$(wc -l < "$BLOCK_OUT")
          echo "Valid: $VALID / $TOTAL"
          rm -f "$BLOCK_TMP" "$RESOLVERS_FILE" "$SERVFAIL_TMP"

      - name: Commit
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: auto-update blocklists"
          file_pattern: 'blocklists.txt'
